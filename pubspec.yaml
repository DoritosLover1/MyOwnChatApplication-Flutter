name: sendbird_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sendbird_chat_sdk: ^4.10.0
  http: ^1.2.1
  cupertino_icons: ^1.0.8
  video_thumbnail: ^0.5.6
  supabase_flutter: ^2.8.0
  sqflite: ^2.3.0
  path: ^1.9.0
  flutter_secure_storage: ^9.2.2
  url_launcher: ^6.3.2
  flutter_dotenv: ^6.0.1
  file_picker: ^11.0.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env